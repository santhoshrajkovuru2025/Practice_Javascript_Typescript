// TSC
// Type Assertions: telling the compiler the type of the paramete or variable.
// 1. using <>
// 2. using 'as' keyword.
var total = 123;
var totalBill = total + 200;
console.log(totalBill);
var usingValue = "hello Typescript Learning with Sunthosh";
var len = usingValue.length;
console.log(len);
var flag = true;
if (flag) {
    console.log('Hello TS !!!');
}
