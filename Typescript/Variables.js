// both the type and intial value:
var empName = 'sunthosh';
console.log(empName);
console.log('-----------------------');
// only the type:
var empId;
empId = 1984;
console.log(empId);
console.log('-----------------------');
// only the initialization of the value (no type)
var empDept = 'QA';
console.log(empDept);
// No type and the initialize the value
var empLocation;
empLocation = 'Hyderabad';
console.log(empLocation);
